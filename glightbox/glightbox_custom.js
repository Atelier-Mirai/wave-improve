/*=====================================================================
  GLightbox 設定用

  selectorプロパティに、
    画像ギャラリー、説明書き、動画再生、インライン表示用の
    クラス名を指定します
=====================================================================*/

GLightbox({
  selector: ".glightbox"
})

GLightbox({
  selector: ".glightbox-with-description"
})

GLightbox({
  selector: ".glightbox-videos-gallery"
})

GLightbox({
  selector: ".glightbox-inline"
})
